# OptiMPa — Phase 2: FastAPI Server Dependencies
# Install with: pip install -r requirements.txt

# ── Core API ──────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0   # ASGI server with websocket + http/2 support
pydantic>=2.6.0             # Data validation — v2 required for field_validator

# ── ML Runtime ────────────────────────────────────────────
scikit-learn>=1.3.0         # Must match the version used in training
joblib>=1.3.0               # Model deserialization
numpy>=1.24.0               # Feature vector construction
xgboost>=1.7.0,<2.0.0
shap>=0.42.0

# ── Utilities ─────────────────────────────────────────────
python-multipart>=0.0.9     # Required by FastAPI for form data (future-proofing)
